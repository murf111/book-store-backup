# ============================================
# ENVIRONMENT VARIABLES - WORKING DEFAULTS
# ============================================
# These defaults work out-of-the-box for local development

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=bookstore
DB_USER=admin
DB_PASSWORD=secret

# JWT Configuration (256-bit secret - change for production!)
JWT_SECRET=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
JWT_EXPIRATION=3600000

# Email Configuration (Optional - for order confirmation emails)
# Using Gmail as example. Get app password from: https://myaccount.google.com/apppasswords
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-16-digit-app-password

# ============================================
# NOTES FOR FUTURE DEVELOPER:
# ============================================
#
# For Quick Evaluation:
# - Just run: mvn spring-boot:run -Dspring.profiles.active=dev
# - No need to create .env file - app has working defaults
# - Dev mode uses H2 database (no PostgreSQL needed)
#
# For Production Testing (with PostgreSQL):
# 1. Copy this file: cp .env.example .env
# 2. Start database: docker-compose up -d
# 3. Run app: mvn spring-boot:run -Dspring.profiles.active=prod
#
# Email is OPTIONAL - app works without it, order emails just won't send
